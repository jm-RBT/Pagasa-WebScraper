# Core dependencies (Python 3.8.10 compatible)
pdfplumber>=0.7.0,<1.0.0
requests>=2.25.0,<3.0.0
pandas>=1.3.0,<2.0.0
psutil>=5.4.0,<6.0.0
pillow>=8.0.0,<11.0.0
pypdfium2>=4.0.0,<5.0.0
beautifulsoup4>=4.9.0,<5.0.0

# Optional OCR dependencies (for image-based PDFs)
# Install these if you need to process scanned/image-based PDFs
pytesseract>=0.3.8,<1.0.0
pdf2image>=1.14.0,<2.0.0
